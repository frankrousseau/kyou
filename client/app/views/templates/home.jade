#menu.clearfix
    .right
        h1
            a(href="#") 
                img(src='icons/main_icon_small.png')

    .left 
        input#datepicker
#content.pa2.trackers
    .line.pl1
        textarea#dailynote(placeholder="add a note for today")

    .line#zoomtracker
        .line.graph-section
            h2.zoomtitle No tracker selected
            p.zoomexplaination.explaination
            p.zoom-editable
                input.zoomtitle No tracker selected
            p.zoom-editable
                textarea.zoomexplaination.explaination
            p
                select#zoomtimeunit
                    option(value="day") day
                    option(value="week") week
                    option(value="month") month
                span &nbsp;
                select#zoomstyle
                    option(value="line") lines
                    option(value="bar") bars
                    option(value="scatterplot") points
                    option(value="lineplot") lines + points
                    option(value="correlation") correlate (points)
            p
                select#zoomcomparison
                span.smaller.em &nbsp;(Compared tracker is in red).
        #zoomgraph.graph-container
            #zoom-y-axis.y-axis
            #zoom-charts.chart
            #timeline.rickshaw_annotation_timeline
        .line.txt-center.pt2
            a(href="#") go back to tracker list
        p
            button#remove-btn.smaller remove tracker
        p
            button#show-data-btn show data
        #raw-data

.tools.line
    #add-tracker-widget
        h2 Create your tracker
        .line
            input#add-tracker-name(placeholder="name")
        .line
            textarea#add-tracker-description(placeholder="description")
        .line
            button#add-tracker-btn add tracker
